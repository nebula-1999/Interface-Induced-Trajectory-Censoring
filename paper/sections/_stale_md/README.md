# 陈旧文件，勿用

这些 `.md` 是 2026-08-31 用 `convert.py` 从 `writeup/paper_draft.md` 切出的
中间产物，曾经是 `.tex` 的来源。

**它们现在已经过时**，缺少后来直接在 `.tex` 上做的全部修改：
补回的 §5.6.1、重写的 §7 Limitations、overclaim 降级、
人工验证部分的重写、以及排版修复。

方向已反转：`paper/sections/*.tex` 是唯一事实来源，
`writeup/paper_draft.md` 由 `paper/regen_draft.py` 派生。

保留它们只为存档 pandoc 的转换失败案例（见 ERRATA 与 git 历史里
被三反引号打断的四家族表）。**不要从这里重新生成任何东西。**
