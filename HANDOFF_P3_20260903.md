# P3 交接（→ Codex），2026-09-03

**任务**：broken-FC vs repaired-FC 的 RL 单变量对照。两条臂只差 verl parser
registry 里的一次注册。

**为什么值得做**：这是全文唯一一个**能推翻我们自己已写下的主张**的实验
（§5.9 的 "a working channel is necessary, not sufficient"）。两种结果都要报，
而且推翻那种对论文更有价值。

完整方案见 `p6/PLAN_P3.md`。本文件只写接手需要的东西。

---

## 0. 机器现状（2026-09-03 11:00）

- `autodl-code` **ssh connection refused**——τ-bench 全量跑完后实例像是已关机。
  开机后先 `df -h`：跑 τ-bench 时数据盘 `/root/autodl-tmp` 只剩 **3.9 GB**（99% 满）。
- τ-bench 两臂结果已拉到本地 `p5/results_full/`，服务器上那两个 json 可以删。

## 1. 必须先做的步骤 0（几分钟，不做别往下走）

verl 的 AgentLoop **自带一套 ToolParser 注册表**
（`verl.experimental.agent_loop.tool_parser`），由
`actor_rollout_ref.rollout.multi_turn.format` 按名字选择，
注册方式是 `@ToolParser.register(name)` 类装饰器。

**验证两件事**：
1. `multi_turn.format` 不是被校验成固定枚举（config schema 里可能有 allowlist）；
2. 自定义注册名能被 `get_tool_parser(name)` 取到。

**退路**：若被枚举卡死，直接 monkey-patch `HermesToolParser.extract_tool_calls`。
`p3/rollout_probe.py` + `p3/sitecustomize.py` 已经证明该点能稳定拦截——115 次生成
全部截获，0 遗漏。这套钩子直接复用，不要重写。

> 注意 verl 的 TaskRunner 是独立 Ray actor 进程，钩子必须走 `sitecustomize.py` +
> `builtins.__import__`，直接 import 装不上。`p3/sitecustomize.py` 里已经处理好了，
> 且拆成 `install()`（parser）和 `install_agentloop()`（`_call_tool`）两段——因为
> `tool_parser` 导入完成时 `tool_agent_loop` 只是部分初始化。

## 2. 必须在 7B 上做，且必须用 LoRA

**不能在 1.5B 上做。** 探针实测：1.5B 在训练提示词下 **0/52 的输出点名
`run_tests`**。修好 parser 也没有东西可接受，repaired 臂必然仍是零工具执行——
和 Granite 臂被判「无信息量」是同一个道理，做了等于没做。

会发出合规调用的是 **7B**：rollout 探针 115 次生成里 **45 条**是合法完整的裸 JSON 调用。

但 7B **全参 RL 在单张 80 GB 上 CUDA OOM**（实测，OOM 发生在 rollout 之后的 actor
更新阶段，余量只剩 890 MiB）。所以：

**两条臂都用 LoRA，配置完全相同。** 这不影响臂间对比（P3 问的是臂间差），
但**与历史 150 步全参 baseline 不严格可比**——这句必须写进论文，不能默认。

## 3. 实验设计

固定：同模型（Qwen2.5-Coder-7B）、同数据、同种子、同步数、同 LoRA 配置、同奖励函数。

| 臂 | `multi_turn.format` | 预期 |
|---|---|---|
| broken-FC | `hermes` | 工具执行 ≈ 0（复现 §5.8） |
| repaired-FC | 自定义 bare-JSON parser | 工具执行 > 0 |

**数据文件是个坑**：用 `train_fc3.parquet`（`tool_agent` + FC_MANDATORY 提示词）。
**`train.parquet` 已被 ReAct run 覆盖**，里面烘进去的是 `agent_name: react_agent`。
开跑前加一条自检：读 parquet 第一行确认 `agent_name == "tool_agent"`，不对就退出。

**每步必须记录**：工具执行次数、`num_turns` 分布、多轮救回数、
emitted/parsed/executed 三层计数（复用 `p3/rollout_probe.py` 的钩子）。

## 4. 开跑前的两笔欠账（论文 §7 已写明，必须先还）

1. **修验证器两处缝隙**：把 `(\d+) passed` 正则锚定到 pytest 自己的 summary 行；
   把 `skipped` / `xfail` 计入分母。
2. **保留完整 rollout 文本**。历史 run 没存，导致「零执行 ≠ 零尝试」无法区分——
   这是 §7 item 11 明确写下的欠账，这次必须补上。

## 5. 结果落地后论文怎么改

**分支 A：repaired-FC 恢复了多轮学习** → §5.9 的 sufficiency 主张**是错的，必须撤回**。
要改：§5.9 整节、§7 item 8 与 12、§6 cold-start 段、§8、摘要末段。
撤回要写得干脆，不要辩解——沿用撤回 p=0.648 的语气。

**分支 B：没有恢复** → sufficiency 结论从「一条 ReAct 臂」升级为「两条臂，
其中一条是单变量的」。要改：§5.9 加入这条臂并写明它就是原文说无法构造的那个
parser-repair 对照；§7 item 8 从「没有干净对照」改为「该对照现已存在，结论为 X」；
「What would change our conclusions」第一条从待办移入已完成。

**两个分支共同**：PEFT 说明必须写清（见第 2 节末）；若保留了 rollout 原文，
§7 item 11（零执行 ≠ 零尝试）和 item 14 的审计边界（"full rollout text was not
retained"）都要相应更新。

## 6. 现成资产

```
p3/rollout_probe.py     # 钩子 + 三层计数，115 次生成 0 遗漏
p3/sitecustomize.py     # Ray actor 里装钩子；install() / install_agentloop() 两段
p3/summarise_p3.py      # 汇总
p3/run_p3_probe.sh      # 自证式：FIRED 计数为 0 就写 P3_INVALID
p3/discover_verl.py     # 探 verl 内部结构
p6/PLAN_P3.md           # 完整方案，含三条显存路线的取舍
```

`run_p3_probe.sh` 的自证纪律要保留：**任何一个钩子触发次数为 0 就判 INVALID**。
这条救过一次——早期版本钩子没装上但脚本"成功"退出。

## 7. 成本

两条臂各 75–150 步，LoRA 下单臂约 4–8 小时，合计 8–16 小时。
**先算磁盘再开跑**——数据盘只剩 3.9 GB，LoRA checkpoint 虽小，仍需腾空间或把
`default_local_dir` 指到系统盘（还有约 17 GB）。
