# Code Agent GPU handoff — 2026-08-27

## Stop state

- User requested an immediate stop. Do not resume automatically.
- `autodl-code` SSH alias now points to port `56557`.
- Instance: `1× NVIDIA A800 80GB PCIe`; GPU was idle at handoff (`0%`, `0 MiB`).
- No training was started.
- The only active operation was `pip install --dry-run`; it was stopped. Because it was a dry run, no heavy training packages were installed.
- The GPU instance remains powered on and billable until the user shuts it down in the AutoDL console.

## Completed and preserved on the server

- Project: `/root/autodl-tmp/code-agent`
- Models downloaded:
  - `/root/autodl-tmp/models/Qwen2.5-Coder-1.5B-Instruct` (~2.9 GB)
  - `/root/autodl-tmp/models/Qwen2.5-1.5B-Instruct` (~2.9 GB)
- `verified_ids.json`: **7,414** usable KodCode records (requirement ≥2,600).
- `probes_repair.jsonl`: **456** repair probes (requirement ≥450).
- Tests: **37 passed** (`test_sandbox.py`, `test_code_tool_core.py`, `test_eval_decompose.py`).
- GPU setup output is in `runall.done`; dependency dry-run output is in `install-dry-run.log`.

## Unresolved blocker: EvalPlus verification

`evalplus_bad.json` contains three cases:

1. `HumanEval/32`: still fails with 4 GB memory. The HF dataset test ends with
   `assert _poly(*candidate(*inp), inp) <= 0.0001`, which is incompatible with the canonical solution returning a float. Investigate the official EvalPlus evaluator/source instead of silently changing the benchmark.
2. `Mbpp/255`: passes when rerun with `mem_mb=4096`; the default 512 MB sandbox limit is too low for this case.
3. `Mbpp/599`: not resolved. Rerun alone with a larger memory limit and longer timeout, then decide whether the official test is pathological.

Do not claim `542/542` until these cases are resolved and the full verification is rerun.

## Versions to reproduce from the working paper instance

- Python 3.12
- PyTorch `2.13.0+cu130`
- vLLM `0.27.1`
- verl `0.9.0`
- transformers `5.10.4`
- ray `2.57.0`
- datasets `5.0.1`

The new instance currently uses `/root/code-venv`. Cache paths are configured by `/root/autodl-tmp/env.sh`.

## Resume order

1. Ask the user to power on the GPU and provide the new SSH port if it changed.
2. Resolve the three EvalPlus cases and rerun `verify_evalplus.py`.
3. Run the dependency dry run again, then install the pinned training stack.
4. Generate the parquet training data.
5. Run `./run_code_grpo.sh smoke`; verify tool calls work and reward is not all zero.
6. Run the two-model baseline and select the base model before any 150-step training run.

