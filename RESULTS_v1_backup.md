# Turn-1 还是 Turn-N：小模型 code agent 的多轮 RL 增益归因

> **一句话**：在"写代码 → 跑测试 → 读报错 → 修"的多轮环境上做 GRPO，
> 端到端 pass@1 提升的 **94.4% 来自初稿质量**；靠多轮修复救回的题数在
> 150 步训练前后从 8 题变成 9 题。模型**确实**学会了修正已知错误
> （scripted 探针 +4.6%，p=0.030），但那种能力在真实失败上无处施展——
> 因为失败的题不是"差一点"，是"完全不会"。

**实验规模**：Qwen2.5-Coder-1.5B-Instruct，单卡 A800，150 步 GRPO，
2 个独立训练 seed。评测集 540 题（HumanEval+ ∪ MBPP+，剔除 2 条已确认的
EvalPlus 数据缺陷）。GPU 总花费约 ¥56。

---

## 1. 主结果

### 1.1 端到端与分解（McNemar 配对检验，step 0 → 150）

三个独立 run：两个训练 seed 的 GRPO，以及同超参下只换 advantage 估计器的 RLOO。

| run | 指标 | step 0 | step 150 | Δ | p |
|---|---|---:|---:|---:|---:|
| GRPO seed42 | final_pass | 64.8% | 67.5% | +2.8% | 0.064 |
| | turn1_pass | 63.3% | 65.9% | +2.6% | 0.093 |
| | repair_rate | 56.8% | 61.5% | +4.6% | 0.030 \* |
| GRPO seed1 | final_pass | 64.2% | 67.0% | +2.8% | 0.078 |
| | turn1_pass | 62.9% | 65.9% | +3.0% | 0.053 |
| | repair_rate | 57.5% | 61.2% | +3.7% | 0.097 |
| RLOO seed42 | final_pass | 64.6% | 67.2% | +2.6% | 0.077 |
| | turn1_pass | 63.1% | 65.7% | +2.6% | 0.082 |
| | repair_rate | 56.8% | 57.7% | +0.9% | 0.786 |

**端到端提升在三个 run 上高度一致（+2.6% ~ +2.8%），但均未达显著（p≈0.06–0.08）。**

**`repair_rate` 的变化不稳定**：+0.9% ~ +4.6%，跨 run 摆动幅度超过效应本身。
早期我曾把 GRPO seed42 那个 p=0.030 当作"RL 教会了模型修正错误"的证据，
**RLOO 的 +0.9%（p=0.786）推翻了它**。本文不把它作为发现。

### 1.2 核心分解 —— 主结论

**step 150 新通过的题里，绝大多数是第一轮就过的：**

| run | 新通过 | 其中首轮就过 | 占比 | 靠多轮救回 |
|---|---:|---:|---:|---:|
| GRPO seed42 | 36 | 34 | **94.4%** | 2 |
| GRPO seed1 | 39 | 36 | **92.3%** | 3 |
| RLOO seed42 | 34 | 31 | **91.2%** | 3 |

多轮救回的绝对题数（542 题中）在**三个 run、11 个评测点、共 33 次测量**中，
全部落在 **5–10 题**之间，且均无趋势：

```
step      0  15  30  45  60  75  90 105 120 135 150
GRPO/42   8   7   5   7   5   7   8   9   9   8   9
GRPO/1    7   9   6   6   7   6  10   8   9   8   6
RLOO/42   8   7   6   7   7   7   7   5   6   6   8
```

**150 步训练没有让模型多救回哪怕一道题。**

### 1.3 稳定性：跨 seed 与跨算法

seed 同时作用于数据采样与 rollout 采样，因此是**含训练方差**的误差棒。

| | Δfinal | Δturn-1 | 首轮占比 |
|---|---:|---:|---:|
| GRPO seed42 | +2.8% | +2.6% | 94.4% |
| GRPO seed1 | +2.8% | +3.0% | 92.3% |
| **RLOO seed42** | **+2.6%** | **+2.6%** | **91.2%** |
| 极差 | 0.2% | 0.4% | 3.2% |

**换训练 seed、换 advantage 估计器，每一个主指标都几乎不动。** 主结论因此不是
单次观察，也不限于 GRPO——它是 critic-free outcome-only RL 的性质。

---

## 2. 机制：为什么多轮没用

### 2.1 失败的题是"完全不会"，不是"差一点"

baseline 阶段的轨迹分析（1992 条）：

- 跑满 4 轮仍失败的题，**100% 在第一轮就零测试通过**
- 其中 76% 模型确实改了代码——**不是不改，是改了也不对**
- 而同一个模型在 scripted 探针上（给定几乎正确的解 + 单点错误）能修好 **61%**

模型**有**修复能力，只是那些题超出它的能力边界。

### 2.2 轨迹高度两极化

```
1 轮结束: 973      4 轮跑满: 921      2 轮: 76      3 轮: 22
```

**要么第一次就对，要么怎么改都不对**，中间状态只占 4.9%。

### 2.3 排除替代解释：不是 prompt 写得不好

同一个 step-150 checkpoint、同一批 542 题，只把失败后的追问从
"请修正代码"换成"请先分析报错的根本原因、你哪一步逻辑出了问题，
想清楚再给出修改"：

| 指标 | 默认追问 | 强引导 | Δ | p |
|---|---:|---:|---:|---:|
| final_pass | 66.61% | 66.79% | +0.18% | 1.000 |
| turn1_pass | 64.39% | 64.02% | −0.37% | 0.683 |
| **多轮救回** | 2.21% (12 题) | 2.77% (15 题) | +0.55% | **0.505** |

**显式的推理引导推不动多轮救回率。** 这排除了"引导不足"，
支持"能力边界"的解释——也意味着用 SFT / 蒸馏 CoT 去教同样的行为，先验很低。

### 2.4 按 bug 类型的修复率：符号错好修，逻辑缺失难修

| 好修 | | 难修 | |
|---|---:|---|---:|
| swap_args（实参写反） | 56.9% | drop_guard（漏掉边界判断） | 33.3% |
| cmp_invert（比较方向反） | 52.6% | boolop_flip（and/or 用反） | 27.3% |
| const_off_by_one（常量差一） | 46.0% | **cmp_boundary（< ↔ <=）** | **20.0%** |

---

## 3. 附带发现

**3.1 代码专用模型初稿更强，但更不会 debug。** Coder-1.5B 的 turn-1 比通用
1.5B 高 7 个点，多轮增量却只有它的一半（3.1% vs 6.3%），修复率低 6 个点。
Coder 版有 **23.5%** 的失败题从头到尾一个字都没改（反复提交同一份代码），
通用版只有 3.3%。

**3.2 多轮的价值由"能力边界"与"重试倾向"共同决定。** 未训练 base model 的横向对比：

| 模型 | turn-1 | final | gap | 救回题数 | repair |
|---|---:|---:|---:|---:|---:|
| Qwen2.5-1.5B（通用） | 49.1% | 55.4% | 6.3% | 34 | 53.5% |
| Qwen2.5-Coder-1.5B | 56.1% | 59.2% | **3.1%** | 17 | 47.1% |
| Qwen2.5-Coder-7B | 73.8% | 80.6% | **6.8%** | 37 | **82.4%** |

同系列 1.5B → 7B：**gap 翻 2.2 倍**（救回 17 → 37 题），repair **+35 个点**
——§2.1 机制解释的可证伪预测（能力上升则更多失败落进"差一点"区间）**被证实**。

**但 gap 不是能力的单调函数**：通用 1.5B 的 gap（6.3%）反而高于同规模的 Coder 版
（3.1%），因为它更愿意重试（Coder 版 23.5% 的失败题一字未改，通用版仅 3.3%）。
单调的是 repair_rate：47.1% → 53.5% → 82.4%。

**3.3 RL 后代码变短 37%**（长度中位 280 → 175），跑满 4 轮的轨迹从 194 减到 176。
GRPO 在 outcome-only reward 下隐式偏好短轨迹——与"增益全在 turn-1"互相印证。

---

## 4. 方法学备注

**4.1 单点 CI 会让你错过配对检验能检出的效应。** 训练中途我曾用 454 题的
单点 95% CI（±4.6，非配对）判定 repair 的提升是噪声；配对检验却显示
z=2.17、p=0.030 显著。**这正是"没有论文发布 per-task 结果，外部无人能做
配对检验"这一问题的反面印证**：不发 per-task 数据，读者连这个效应存在都看不到。

**4.2 turn-1 与 final 是嵌套关系**（通过就不再改），不可能"turn-1 对而 final 错"，
McNemar 的 b 恒为 0，检验退化成单向二项检验——**比跨模型比较灵敏一个量级**。
Step 0 算的 MDD=6.0 只适用于跨模型比较。

**4.3 batch=16 下步间奖励方差 ±0.15**，完全淹没模型能力的真实变化。
任何基于单点或少数几步的训练曲线解读都是在读噪声。

**4.4 评测环境会影响绝对数值。** 同一模型在独立 vLLM 与 verl rollout 下
相差 5–10 个点（疑似 chat template 差异）。**所有对比都必须在同一环境内进行**；
本报告的主结果全部取自 verl rollout 环境。

---

## 5. 复现

```bash
python analyze_all.py        # 报告中全部四张表（主检验/稳定性/算法/规模）
python analyze_baseline.py   # 机制分析（轨迹分解、bug 类型拆分）
```

per-task per-turn 原始数据：`runs/step3/`（step 0–150 每个评测点）、
`runs/probe/`（prompt 干预实验）。**每道题每一轮的代码、观测、通过情况全部保留**
——据我们所知这是该方向第一份公开的 per-task 多轮轨迹数据。

## 6. 局限

- 单一模型规模做 RL（1.5B）、单数据集（KodCode 训练 / EvalPlus 评测）、
  2 个训练 seed + 1 个算法对照
- 总提升 +2.8% 未达显著（p=0.064）；主结论依赖的是分解比例而非总提升幅度
- 未做 SFT / 蒸馏冷启动（§2.3 的干预实验给出了跳过它的依据，但非直接证伪）
- 已验证 RLOO（critic-free）复现同一结论；**未验证有 learned critic 的 PPO**。
  该验证需要独立的超参搜索（critic lr / GAE lambda），因而无法保持"只换算法"
  的干净对照，留作 future work
- 规模对比（§3.2）只有 base model 评测，未在 7B 上重跑 RL——受预算限制
